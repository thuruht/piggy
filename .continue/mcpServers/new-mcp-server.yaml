name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: New MCP server
    command: npx
    args:
      - -y
      - mcp-remote
      - --mcp-server-url
      - https://casb.mcp.cloudflare.com/sse
    env: {}
